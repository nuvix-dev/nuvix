export { defaultProjectSettings } from "./defaults";
export { resolveSettings } from "./resolve";
export type {
	DeepPartial,
	ProjectAuthSettings,
	ProjectLimitSettings,
	ProjectSettings,
} from "./types";
